<UserControl x:Class="XDeploy.Views.CreateDeploymentTargetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <Border Style="{StaticResource PageTitle}">
            <TextBlock>Create New Deployment Target</TextBlock>
        </Border>
        <ContentControl x:Name="Form" />
        <Border Style="{StaticResource FormActions}">
            <StackPanel>
                <Button x:Name="Save" Content="Save" Style="{StaticResource ActionButtonPrimary}" />
                <Button x:Name="Cancel" Content="Cancel" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
