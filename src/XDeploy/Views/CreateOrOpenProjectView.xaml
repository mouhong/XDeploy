<UserControl x:Class="XDeploy.Views.CreateOrOpenProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Visibility="{Binding Visibility}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Content="Create Project" Grid.Row="1" Grid.Column="0" Margin="5" Height="50" VerticalAlignment="Center" />
        <Button Content="Open Project" Grid.Row="1" Grid.Column="1" Margin="5" Height="50" VerticalAlignment="Center" Command="{Binding OpenProjectCommand}" />
    </Grid>
</UserControl>
