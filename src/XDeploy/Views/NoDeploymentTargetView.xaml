<UserControl x:Class="XDeploy.Views.NoDeploymentTargetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-120,0,0">
        <Border Style="{StaticResource MessageInfo}">
            <TextBlock>
                There' no deploy target right now.
            </TextBlock>
        </Border>
        <Button x:Name="CreateNewTarget" Content="Create New Target" Style="{StaticResource ButtonLarge}" />
    </StackPanel>
</UserControl>
