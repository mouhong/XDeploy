<UserControl x:Class="XDeploy.Workspace.DeploymentTargets.Screens.EditDeploymentTargetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <HeaderedContentControl Header="Edit Deployment Target" Style="{StaticResource PageBody}">
    <StackPanel>
        <ContentControl x:Name="Form" />
        <Border Style="{StaticResource FormActions}">
            <StackPanel>
                    <Button x:Name="Save" Content="Save" Style="{MultiStyle Btn BtnPrimary BtnInFormActions}" />
                    <Button x:Name="Cancel" Content="Cancel" Style="{MultiStyle Btn BtnInFormActions}" />
            </StackPanel>
        </Border>
        </StackPanel>
    </HeaderedContentControl>
</UserControl>
