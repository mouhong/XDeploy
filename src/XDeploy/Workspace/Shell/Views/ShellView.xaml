<UserControl x:Class="XDeploy.Workspace.Shell.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             MinWidth="800" MinHeight="600"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_New" />
                <MenuItem x:Name="OpenProject" Header="_Open" />
                <Separator />
                <MenuItem x:Name="CloseProject" Header="_Close Project" />
                <Separator />
                <MenuItem x:Name="Exit" Header="_Exit" />
            </MenuItem>
        </Menu>
        <xctk:BusyIndicator Grid.Row="1" IsBusy="{Binding Busy.IsBusy}" BusyContent="{Binding Busy.Message}">
            <ContentControl x:Name="ActiveItem" />
        </xctk:BusyIndicator>
    </Grid>
</UserControl>
